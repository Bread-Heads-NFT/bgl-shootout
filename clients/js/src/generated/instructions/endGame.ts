/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  PublicKey,
  Serializer,
  Signer,
  TransactionBuilder,
  mapSerializer,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { addObjectProperty, isWritable } from '../shared';

// Accounts.
export type EndGameInstructionAccounts = {
  /** The PDA of the game */
  gamePda: PublicKey;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The system program */
  systemProgram?: PublicKey;
};

// Data.
export type EndGameInstructionData = {
  discriminator: number;
  matchName: string;
  mint: PublicKey;
};

export type EndGameInstructionDataArgs = { matchName: string; mint: PublicKey };

export function getEndGameInstructionDataSerializer(
  context: Pick<Context, 'serializer'>
): Serializer<EndGameInstructionDataArgs, EndGameInstructionData> {
  const s = context.serializer;
  return mapSerializer<EndGameInstructionDataArgs, any, EndGameInstructionData>(
    s.struct<EndGameInstructionData>(
      [
        ['discriminator', s.u8()],
        ['matchName', s.string()],
        ['mint', s.publicKey()],
      ],
      { description: 'EndGameInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<EndGameInstructionDataArgs, EndGameInstructionData>;
}

// Args.
export type EndGameInstructionArgs = EndGameInstructionDataArgs;

// Instruction.
export function endGame(
  context: Pick<Context, 'serializer' | 'programs' | 'payer'>,
  input: EndGameInstructionAccounts & EndGameInstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = {
    ...context.programs.getPublicKey(
      'bglShootout',
      'DUCKdJdA2Hexw9ZS7M5rcDfJneVF3ptQF1NDEJ9RMTbj'
    ),
    isWritable: false,
  };

  // Resolved inputs.
  const resolvingAccounts = {};
  const resolvingArgs = {};
  addObjectProperty(resolvingAccounts, 'payer', input.payer ?? context.payer);
  addObjectProperty(
    resolvingAccounts,
    'systemProgram',
    input.systemProgram ?? {
      ...context.programs.getPublicKey(
        'splSystem',
        '11111111111111111111111111111111'
      ),
      isWritable: false,
    }
  );
  const resolvedAccounts = { ...input, ...resolvingAccounts };
  const resolvedArgs = { ...input, ...resolvingArgs };

  // Game Pda.
  keys.push({
    pubkey: resolvedAccounts.gamePda,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.gamePda, true),
  });

  // Payer.
  signers.push(resolvedAccounts.payer);
  keys.push({
    pubkey: resolvedAccounts.payer.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.payer, true),
  });

  // System Program.
  keys.push({
    pubkey: resolvedAccounts.systemProgram,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.systemProgram, false),
  });

  // Data.
  const data =
    getEndGameInstructionDataSerializer(context).serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
