/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  PublicKey,
  Serializer,
  Signer,
  TransactionBuilder,
  mapSerializer,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { addObjectProperty, isWritable } from '../shared';
import { Action, ActionArgs, getActionSerializer } from '../types';

// Accounts.
export type PlayRoundInstructionAccounts = {
  /** The PDA of the game */
  gamePda: PublicKey;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The Slot_Hashes System Variable */
  slotHashesSysvar?: PublicKey;
  /** The system program */
  systemProgram?: PublicKey;
};

// Data.
export type PlayRoundInstructionData = {
  discriminator: number;
  matchName: string;
  mint: PublicKey;
  action: Action;
};

export type PlayRoundInstructionDataArgs = {
  matchName: string;
  mint: PublicKey;
  action: ActionArgs;
};

export function getPlayRoundInstructionDataSerializer(
  context: Pick<Context, 'serializer'>
): Serializer<PlayRoundInstructionDataArgs, PlayRoundInstructionData> {
  const s = context.serializer;
  return mapSerializer<
    PlayRoundInstructionDataArgs,
    any,
    PlayRoundInstructionData
  >(
    s.struct<PlayRoundInstructionData>(
      [
        ['discriminator', s.u8()],
        ['matchName', s.string()],
        ['mint', s.publicKey()],
        ['action', getActionSerializer(context)],
      ],
      { description: 'PlayRoundInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 2 })
  ) as Serializer<PlayRoundInstructionDataArgs, PlayRoundInstructionData>;
}

// Args.
export type PlayRoundInstructionArgs = PlayRoundInstructionDataArgs;

// Instruction.
export function playRound(
  context: Pick<Context, 'serializer' | 'programs' | 'payer'>,
  input: PlayRoundInstructionAccounts & PlayRoundInstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = {
    ...context.programs.getPublicKey(
      'bglShootout',
      'DUCKdJdA2Hexw9ZS7M5rcDfJneVF3ptQF1NDEJ9RMTbj'
    ),
    isWritable: false,
  };

  // Resolved inputs.
  const resolvingAccounts = {};
  const resolvingArgs = {};
  addObjectProperty(resolvingAccounts, 'payer', input.payer ?? context.payer);
  addObjectProperty(
    resolvingAccounts,
    'slotHashesSysvar',
    input.slotHashesSysvar ??
      publicKey('SysvarS1otHashes111111111111111111111111111')
  );
  addObjectProperty(
    resolvingAccounts,
    'systemProgram',
    input.systemProgram ?? {
      ...context.programs.getPublicKey(
        'splSystem',
        '11111111111111111111111111111111'
      ),
      isWritable: false,
    }
  );
  const resolvedAccounts = { ...input, ...resolvingAccounts };
  const resolvedArgs = { ...input, ...resolvingArgs };

  // Game Pda.
  keys.push({
    pubkey: resolvedAccounts.gamePda,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.gamePda, true),
  });

  // Payer.
  signers.push(resolvedAccounts.payer);
  keys.push({
    pubkey: resolvedAccounts.payer.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.payer, true),
  });

  // Slot Hashes Sysvar.
  keys.push({
    pubkey: resolvedAccounts.slotHashesSysvar,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.slotHashesSysvar, false),
  });

  // System Program.
  keys.push({
    pubkey: resolvedAccounts.systemProgram,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.systemProgram, false),
  });

  // Data.
  const data =
    getPlayRoundInstructionDataSerializer(context).serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
